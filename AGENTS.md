# AGENTS.md

Start with `README.md` — layout, deployment prerequisites, first run, updating, room/key rotation,
and the rotator's config table + web UI.

Further docs live in `docs/`:

- `docs/stack-overview.md` — panel/rotator API and data-flow details.
- `docs/rotator-telemost-click-issue.md` — history of the Telemost create-call-button automation
  issue and how it was diagnosed/fixed.

`TODO.md` tracks open work.

## Branches

`release*` branches auto-mirror to GitHub (as user `antonp-dev`) — no `gh` CLI or GitHub credentials
needed to publish one, push to Forgejo and the mirror follows.
